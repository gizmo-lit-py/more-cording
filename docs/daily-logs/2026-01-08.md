#2026-01-08

-今日やったこと：FLASKをやっていてずっと絶妙にわからんかったからSQL,HTML,ルーティングの問題を何問も解いて基礎を固めた。
-コード：
#問１(SQL)
SELECT *
FROM transactions;

#問２
SELECT *
FROM transactions
WHERE user_id = 1;

#問３
SELECT *
FROM transactions
WHERE category = '食費';

#問４
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT
);

#問５
CREATE TABLE transactions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    category TEXT,
    amount INTEGER,
    created_at TEXT
);

#問６
CREATE TABLE logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    messeage TEXT,
    created_at TEXT
);

#問７
INSERT INTO users (username)
VALUES ('kazuki');

#問８
INSERT INTO transactions (user_id, category, amount, created_at)
VALUES (1, '食費', 1200, '2026-01-01');

#問９
INSERT INTO logs (message, created_at)
VALUES ('login success', '2026-01-02');

#問１０
DELETE FROM logs
WHERE message = 'login success';

#問１１
DELETE FROM transactions
WHERE amount = 1200;

#問１２
DELETE FROM users
WHERE username = 'kazuki';

#問１３
UPDATE transactions
SET category = '外食'
WHERE category = '食費';

#問１４
UPDATE transactions
SET amount = 1500
WHERE amount = 1200;

#問１５
UPDATE transactions
SET category = '外食', amount = 2000
WHERE user_id = 1;

#問１６
SELECT *
FROM transactions
WHERE user_id = 1
ORDER BY created_at DESC;

#問１７
SELECT category, SUM(amount)
FROM transactions
WHERE user_id = 1
GROUP BY category;

#問１８
SELECT username, amount
FROM users
JOIN transactions
ON users.id = transactions.user_id;

#問１９
SELECT username, SUM(amount)
FROM users
JOIN transactions
ON users.id = transactions.user_id
GROUP BY username;

#問２０
SELECT username, COALESCE(SUM(amount), 0)
FROM users
LEFT JOIN transactions
ON users.id = transactions.user_id
GROUP BY username;

#問２１
SELECT username, COALESCE(SUM(amount), 0)
FROM users
LEFT JOIN transactions
ON users.id = transactions.user_id
AND transactions.category = '食費'
GROUP BY username;




